# Replay

1. Inspect `git status --short --untracked-files=all` and `docs/GLOBAL_AGENT_WORKFLOW.md`.
2. Preserve the Codex App mitigation as `ReducedUiControls`: default scale, `--num-raster-threads=2`, `--disable-smooth-scrolling`, no forced reduced-motion.
3. Add subagent delegate-or-waive governance to policy/docs/checkers/tests.
4. Clean only inactive `Documents\Codex` trusted project entries from the user Codex config and refresh provenance reports.
5. Run targeted unit/checker/resource-health commands and bind the resulting evidence in this run directory.
6. Commit in scoped slices, push `newwindow`, and compare local HEAD with upstream.
